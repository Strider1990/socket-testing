{
  "name": "socket-chat",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "scripts": {
    "start:unix": "(cd client && npm run build) && node index.js",
    "start:windows": "(cd client && npm run build) & (cd .. && node index.js)"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "d3": "^5.12.0",
    "express": "^4.17.1",
    "jquery": "^3.4.1",
    "socket.io": "^2.3.0"
  }
}
